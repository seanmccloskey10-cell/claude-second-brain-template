# Briefings

Weekly briefings are saved here — generated when you say *"give me a briefing"*, type `/brief`, or when Claude fires one automatically (it does this if 7+ days have passed since the last one).

Each briefing is a snapshot of your vault at that point in time.

**Don't delete old briefings** — they're a timeline of how your thinking evolved.
